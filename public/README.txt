This directory is for static assets Next.js serves directly (favicon,
og-image.png, etc). No image assets were generated in this build —
this environment does not include an image-generation tool, so a
favicon.ico and public/og-image.png (1200x630, referenced in
src/app/layout.tsx metadata) should be added before launch.
