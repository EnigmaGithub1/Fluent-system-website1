This directory holds the actual product files served by /api/download/[token].
Never expose this directory directly via a public static route.
Place FLUENT-Social-Fluency-System.pdf and FLUENT_COMPLETE.zip here for local dev.
This directory should be in .gitignore in production (files belong in
your object storage, not your git repo).
